# Build settings
[build]
  publish = "."
  command = "echo 'No build required for static site'"

# Redirect rules (optional - for SPA-like behavior)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
